# 📅 Meeting Room Booking System API Specification

เอกสารอธิบายรายละเอียดของ RESTful API Endpoints สำหรับระบบจองห้องประชุม

---

## 🔑 1. Authentication & Users
จัดการเกี่ยวกับตัวตนผู้ใช้งานและการเข้าสู่ระบบ

| HTTP Method | Endpoint | Description | Request Body | Response (Success) | Status |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `POST` | `/api/auth/register` | ลงทะเบียนผู้ใช้ใหม่ | `{username, password, name, role_id}` | `{user_id, message}` | 201 |
| `POST` | `/api/auth/login` | เข้าสู่ระบบเพื่อรับ Token | `{username, password}` | `{token, user_info}` | 200 |
| `GET` | `/api/users/profile` | ดูข้อมูลโปรไฟล์ของตนเอง | - | `{id, name, role}` | 200 |

---

## 🚪 2. Rooms (ห้องประชุม)
การจัดการทรัพยากรห้องประชุม (เจ้าหน้าที่เท่านั้นที่มีสิทธิ์สร้าง/แก้ไข/ลบ)

| HTTP Method | Endpoint | Description | Request Body | Response (Success) | Status |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `GET` | `/api/rooms` | ดึงรายการห้องทั้งหมด | - | `[{room_data}, ...]` | 200 |
| `GET` | `/api/rooms/:id` | ดึงข้อมูลห้องรายห้อง | - | `{room_data}` | 200 |
| `POST` | `/api/rooms` | **(Staff)** เพิ่มห้องใหม่ | `{name, capacity, location}` | `{id, message}` | 201 |
| `PUT` | `/api/rooms/:id` | **(Staff)** แก้ไขข้อมูลห้อง | `{name, capacity, location}` | `{status: "updated"}` | 200 |
| `DELETE` | `/api/rooms/:id` | **(Staff)** ลบห้องออกจากระบบ | - | `{message: "Deleted"}` | 200 |

---

## 🗓️ 3. Bookings (การจอง)
การจัดการธุรกรรมการจองห้องประชุม

| HTTP Method | Endpoint | Description | Request Body | Response (Success) | Status |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `GET` | `/api/bookings` | **(Staff)** ดูรายการจองทั้งหมด | - | `[{booking_data}, ...]` | 200 |
| `GET` | `/api/bookings/my` | ดูประวัติการจองของผู้ใช้ | - | `[{booking_data}, ...]` | 200 |
| `POST` | `/api/bookings` | สร้างการจองใหม่ | `{"room_id", "start_time", "end_time", "purpose"}` | `{id, status: "pending"}` | 201 |
| `DELETE` | `/api/bookings/:id` | ยกเลิกรายการจอง | - | `{message: "Cancelled"}` | 200 |
| `PATCH` | `/api/bookings/:id/status`| **(Staff)** อนุมัติ/ปฏิเสธการจอง | `{"status": "approved"}` | `{id, status: "approved"}` | 200 |

---

## 📝 Example Requests & Responses

### 1. Create Booking (POST `/api/bookings`)
**Request Body:**
```json
{
  "room_id": 101,
  "start_time": "2026-01-15T09:00:00",
  "end_time": "2026-01-15T12:00:00",
  "purpose": "ประชุมกลุ่มวิจัย AI",
  "attendees": 5
}